apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  type: LoadBalancer
  selector:
    app: app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 10051
      targetPort: 10051